import React from "react";
import ReactDOM from "react-dom/client";
import ExStateReact from "./component/ExStateReact";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <div>
    <ExStateReact />
  </div>
);
